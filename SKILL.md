---
name: evidence-based-product-deconstruction
description: 基于产品截图、可访问网站或原始代码等一手证据，先识别产品类型与证据完整度，再从用户层、技术层、模型层和数据层完成可追溯的产品拆解，最终交付自包含 HTML 报告。用于产品逆向分析、竞品拆解、AI 产品架构分析和交互流程审计；没有产品证据时不得启动实质拆解。不要在用户只要文案、实现或通用架构讨论时使用。
---

# 证据驱动产品拆解

## 目标

把当前产品的可观察行为转化为可复核、分层且不越过证据边界的拆解报告。报告必须使用当前产品的真实名称、流程、字段和可追溯结论。不要把其他产品的流程、模型、字段或结论套用到当前对象。不要把本次拆解写回本 skill 的 `SKILL.md`、`references/` 或 `assets/`。

## 强制执行顺序

1. 明确用户希望拆解的对象、目标、范围和交付深度。
2. 识别产品类型；读取 [产品类型路由](references/product-archetypes.md)，据此调整取证重心。
3. 盘点一手证据并执行证据门槛；读取 [取证与分析协议](references/evidence-method.md)。
4. 证据达到当前目标所需的最低条件后，依次分析用户层、技术层、模型层、数据层。
5. 建立跨层数据流、状态流、生产者—消费者关系和冲突清单。
6. 复制 [报告模板](assets/report-template.html)，按 [HTML 交付规范](references/html-output-spec.md) 填入当前产品证据，生成单个自包含 `.html` 文件；在浏览器中打开检查后再交付。

不得跳过产品类型识别或证据门槛。证据不足时，只输出“证据就绪度与缺口”，明确向用户索取截图、网址、录屏导出的关键帧、代码或其他一手材料；不得用宣传文案、常识或相似产品补齐流程。

## 证据门槛

至少要有一种可访问的一手产品证据：

- 覆盖目标流程的连续截图或视频关键帧；
- 可实际浏览且允许只读检查的网站/应用；
- 能支持目标问题的原始代码、构建产物、接口定义或数据结构；
- 用户提供的真实运行日志、导出记录或错误记录。

只有首页、宣传页、转述、零散截图或不可访问链接，不足以支持完整拆解。可以在用户同意的限定范围内做“局部拆解”，但标题、范围和结论必须明确是局部的。

## 产品类型识别

先输出“产品类型判断卡”：主类型与次类型、核心用户任务、主要输入—处理—输出、人机决策权分配、判断依据与置信度、本次加重检查主题。类型不明确但不影响只读取证时，可先收集证据；若不同分类会显著改变范围或交付，先向用户确认。

## 四层拆解

进入每一层前先给出该层的“证据就绪卡”：已有证据、缺少证据、可回答问题、不可回答问题和本层状态（就绪 / 局部就绪 / 未就绪）。就绪后继续；局部就绪时只分析有证据的部分并保留缺口；未就绪时暂停该层并向用户索取最小必要材料。上一层的推断不得自动作为下一层的事实输入。

### 1. 用户层

回答用户是谁、想完成什么、如何进入、如何操作、何时确认或修改、如何失败/中断/恢复、最终拿到什么。至少覆盖用户任务与成功标准、信息架构、时间顺序完整的用户旅程、确认门、正常/修改/异常三类路径、页面反馈、情绪、痛点、信任风险和机会点，并核对聊天声明、任务状态、资产状态与最终输出。

### 2. 技术层

只根据界面、网络、代码或日志等可见证据描述实现边界。至少覆盖前端模块、服务/执行模块、任务编排、工具/外部系统、同步与异步状态机、重试、幂等、取消、权限、计费、资产版本与引用、历史、错误处理、可观测性和状态对账。不得把功能性工具名冒充官方 API，也不得仅凭 UI 推断具体框架、云厂商或数据库。

### 3. 模型层

仅在确有 AI/模型证据时深入，否则说明“不适用或证据不足”。覆盖模型/Agent 的可观察角色、触发、输入、判断、工具、输出、交接、模型选择、模式、上下文、检索、参考资产、确认门、额度、安全、质量验证、失败与重试。区分页面公开计划摘要与真实执行结果。不得声称读取隐藏思维链、官方 System Prompt 或未展示的工具调用；只有用户明确要求时才生成可追溯的“功能等价 Prompt”。

### 4. 数据层

围绕产品对象与状态，而不是猜数据库表。覆盖用户、项目/会话、任务、消息、输入、资产、版本、引用、确认、错误、额度等已出现实体；记录生产者、消费者、读写时机、版本和证据等级；检查依赖、资产血缘、修改失效、局部重算、长期用户信息、项目上下文、运行时结果、状态一致性、权限和敏感数据边界。

## 统一证据语言

每条关键结论必须标为：

- 【页面/代码事实】：一手证据可直接验证；
- 【合理推断】：由多条事实推导，并写明推导依据；
- 【尚未确认】：当前材料不足；
- 【产品建议】：面向改进或目标架构的设计，不冒充现状。

证据编号从 `E01` 起连续递增，截图、代码、日志、页面共用同一序列，不另起前缀。冲突编号从 `C01` 起。证据索引表是唯一来源；表格、流程图、架构图和结论只引用这些编号。产品或 Agent 的自然语言声明不是完成证据，需与资产、状态、代码路径或运行结果交叉验证。冲突必须并列记录，不得自行选一个为事实。

## 操作与安全边界

- 默认只读。发送消息、生成/重生成、批量执行、发布、删除、购买、充值、覆盖资产等动作需用户明确授权。
- 可在范围内滚动、展开、切换只读页面、查看已有预览/历史/代码。
- 登录、验证码或用户接管时暂停并交还界面。
- 不读取或输出 Cookie、Token、密码、浏览器存储或其他凭证。
- 完整拆解不授权探索不相关账号、项目或系统。

## 交付要求

最终交付一个自包含 HTML 主报告；过程笔记不能替代 HTML。章节、文件名、截图嵌入和图例以 [HTML 交付规范](references/html-output-spec.md) 为准，不要另写一套目录。

交付前打开 HTML，检查导航、表格、文字溢出、证据编号、图形和嵌入资源。完成用户指定范围后停止，不自动扩展到 Prompt、代码实现或其他产品。
